# ------- FACTORY RULES
TopRule = $*  ($*) ?

            ($INIT {cmd = 'init'}) ?
            ($READY {cmd = 'next'})

             $*;
READY = ready | (let me know) | (are ready);
INIT = hello | hey | (hey jibo) ;
